<Window x:Class="Sigil.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sigil"
        Height="160"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="Enter name:" Margin="0,0,0,8"/>
        <TextBox x:Name="InputBox" Grid.Row="1" Height="28" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Margin="0,0,8,0" Content="Cancel" Click="OnCancel"/>
            <Button Width="80" Content="OK" IsDefault="True" Click="OnOk"/>
        </StackPanel>
    </Grid>
</Window>
