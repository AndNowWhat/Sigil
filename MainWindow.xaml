<Window x:Class="Sigil.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Sigil"
        mc:Ignorable="d"
        Title="Sigil" Height="680" Width="820"
        MinHeight="580" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#1a1a2e">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="BackgroundDark" Color="#1a1a2e"/>
        <SolidColorBrush x:Key="BackgroundCard" Color="#16213e"/>
        <SolidColorBrush x:Key="BackgroundHover" Color="#1f4068"/>
        <SolidColorBrush x:Key="AccentColor" Color="#e94560"/>
        <SolidColorBrush x:Key="AccentHover" Color="#ff6b6b"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#ffffff"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#a0a0a0"/>
        <SolidColorBrush x:Key="BorderColor" Color="#2a2a4a"/>

        <!-- Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BackgroundCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BackgroundHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentHover}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Small Button Style -->
        <Style x:Key="SmallButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- ListBox Style -->
        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource BackgroundCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer Padding="4">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem Style -->
        <Style TargetType="ListBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BackgroundHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundCard}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Expander Style -->
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="0" Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Accounts -->
            <Grid Grid.Column="0" Margin="0,0,16,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Accounts"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimary}"
                           Margin="0,0,0,12"/>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Accounts}"
                         SelectedItem="{Binding SelectedAccount}"
                         DisplayMemberPath="DisplayName"
                         Style="{StaticResource ModernListBox}"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,12,0,0">
                    <Button Content="+ Add" Style="{StaticResource SmallButton}"
                            Click="OnAddAccount" Margin="0,0,8,0"/>
                    <Button Content="Remove" Style="{StaticResource SmallButton}"
                            Click="OnRemoveAccount"/>
                </StackPanel>
            </Grid>

            <!-- Right Panel - Selected Account & Launch -->
            <Grid Grid.Column="1" Margin="16,0,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Selected Account Header -->
                <StackPanel Grid.Row="0" Margin="0,0,0,16">
                    <TextBlock Text="Selected Account"
                               FontSize="12"
                               Foreground="{StaticResource TextSecondary}"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding SelectedAccount.DisplayName, FallbackValue='No account selected'}"
                               FontSize="24" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"/>
                    <Border Background="{StaticResource BackgroundCard}"
                            BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1" CornerRadius="6"
                            Padding="10,6" Margin="0,8,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Proxy"
                                       FontSize="11" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1"
                                       Text="{Binding ProxyDisplayText}"
                                       FontSize="11"
                                       Foreground="{StaticResource TextPrimary}"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"/>
                            <Button Grid.Column="2"
                                    Content="Configure"
                                    Style="{StaticResource SmallButton}"
                                    Click="OnConfigureProxy"
                                    Padding="10,3"
                                    FontSize="10"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Characters header row -->
                <Grid Grid.Row="1" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               Text="Characters"
                               FontSize="14" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimary}"
                               VerticalAlignment="Center"/>

                    <TextBlock Grid.Column="1"
                               Text="{Binding CharacterCountText}"
                               FontSize="12"
                               Foreground="{StaticResource TextSecondary}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="Refresh"
                                Click="OnRefreshCharacters"
                                Style="{StaticResource SmallButton}"
                                Margin="0,0,4,0"
                                ToolTip="Refresh the characters for this account"/>
                        <Button Content="+ Create"
                                Style="{StaticResource SmallButton}"
                                IsEnabled="{Binding CanCreateCharacter}"
                                Click="OnCreateCharacter"
                                Margin="0,0,4,0"/>
                        <Button Content="Auto"
                                Style="{StaticResource SmallButton}"
                                IsEnabled="{Binding CanAutoCreate}"
                                Click="OnAutoCreateCharacters"
                                ToolTip="Automatically fill this account to 20 characters (30s between each)"/>
                    </StackPanel>
                </Grid>

                <!-- Queue status row -->
                <Grid Grid.Row="2" Margin="0,0,0,8"
                      Visibility="{Binding IsQueueActive, Converter={StaticResource BoolToVisibility}}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding QueueStatusText}"
                               Foreground="#aaaaaa"
                               FontSize="11"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1"
                            Content="Cancel Queue"
                            Click="OnCancelQueue"
                            Style="{StaticResource SmallButton}"
                            Margin="8,0,0,0"/>
                </Grid>

                <ListBox Grid.Row="3"
                         ItemsSource="{Binding SelectedCharacters}"
                         SelectedItem="{Binding SelectedCharacter}"
                         DisplayMemberPath="DisplayLabel"
                         Style="{StaticResource ModernListBox}"/>

                <!-- Launch Button -->
                <Button Grid.Row="4" Content="Launch Game"
                        Style="{StaticResource PrimaryButton}"
                        Click="OnLaunch"
                        Margin="0,16,0,0"
                        Height="50"
                        FontSize="16"/>

                <!-- Advanced Settings Expander -->
                <Expander Grid.Row="5" Header="Advanced Settings" Margin="0,16,0,0" IsExpanded="False">
                    <StackPanel Margin="0,12,0,0">
                        <!-- Client Path -->
                        <TextBlock Text="RS3 Client Path"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondary}"
                                   Margin="0,0,0,6"/>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding Settings.Rs3ClientPath, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"/>
                            <Button Grid.Column="1" Content="Browse"
                                    Style="{StaticResource SmallButton}"
                                    Click="OnBrowseRs3Client"
                                    Margin="8,0,0,0"/>
                        </Grid>

                        <!-- Character creation rate -->
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="80"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Create"
                                       FontSize="12" Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding Settings.CharacterCreationBatchSize, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"/>
                            <TextBlock Grid.Column="2" Text="characters, then wait"
                                       FontSize="12" Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="8,0"/>
                            <TextBox Grid.Column="3"
                                     Text="{Binding Settings.CharacterCreationBatchWindowMinutes, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"/>
                            <TextBlock Grid.Column="4" Text="min"
                                       FontSize="12" Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </Grid>

                        <!-- ProxInject -->
                        <TextBlock Text="Game Proxy (ProxInject)"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondary}"
                                   Margin="0,4,0,6"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock x:Name="ProxInjectStatusText"
                                       Text="Not installed"
                                       FontSize="11"
                                       Foreground="{StaticResource TextSecondary}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <Button x:Name="ProxInjectDownloadBtn"
                                    Content="Download ProxInject"
                                    Style="{StaticResource SmallButton}"
                                    Click="OnDownloadProxInject"/>
                        </StackPanel>

                        <!-- Actions -->
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Refresh Token" Style="{StaticResource SmallButton}"
                                    Click="OnRefreshToken" Margin="0,0,8,0"/>
                            <Button Content="Save Settings" Style="{StaticResource SmallButton}"
                                    Click="OnSaveSettings" Margin="0,0,8,0"/>
                            <Button Content="Token Info" Style="{StaticResource SmallButton}"
                                    Click="OnShowTokenInfo"/>
                        </StackPanel>
                    </StackPanel>
                </Expander>
            </Grid>
        </Grid>

        <!-- Log Panel -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundCard}"
                BorderBrush="{StaticResource BorderColor}" BorderThickness="0,1,0,0"
                Height="120">
            <ScrollViewer x:Name="LogScrollViewer"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Padding="16,6">
                <ItemsControl ItemsSource="{Binding LogLines}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontSize="11"
                                       FontFamily="Consolas"
                                       TextWrapping="NoWrap"
                                       Margin="0,1"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
